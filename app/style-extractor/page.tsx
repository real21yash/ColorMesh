import { StyleExtractor } from '@/components/style-extractor';

export const metadata = {
  title: 'Style Extractor — ColorMesh',
  description: 'Extract colors, fonts, radius, shadows, spacing, and CSS variables from any website URL.',
};

export default function StyleExtractorPage() {
  return <StyleExtractor />;
}
